{
  "name": "futures-algo-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Local setup wizard and webhook bridge for TradingView futures automation.",
  "type": "module",
  "scripts": {
    "local": "node local/server.js",
    "build": "node --check index.js && node --check public/app.js && node --check api/webhook/tradingview.js",
    "check": "node --check index.js && node --check local/server.js && node --check src/config.js && node --check src/router.js"
  },
  "engines": {
    "node": ">=20"
  }
}
